# 🎮 Game Arena

Модульная игровая арена с коллекцией мини-игр. Архитектура спроектирована для легкого добавления новых игр и оптимальной производительности.

## 📁 Структура проекта

```
.
├── index.html              # Главная HTML страница
├── main.js                 # Точка входа, инициализация всех систем
├── config.js              # Конфигурация (Web3, контракты)
├── web3.js                 # Интеграция с блокчейном
├── style.css               # Глобальные стили и стили главного меню
│
├── core/                   # Ядро системы
│   ├── GameBase.js        # Базовый класс для всех игр
│   ├── GameManager.js     # Менеджер игр (переключение, управление)
│   └── EventBus.js        # Система событий для коммуникации
│
├── games/                  # Мини-игры
│   └── frost-click/       # Frost Click игра
│       ├── FrostClickGame.js      # Основной класс (координация)
│       ├── FrostClickRenderer.js  # Модуль рендеринга Canvas
│       ├── FrostClickGameLogic.js # Игровая логика и UI
│       └── frost-click.css
│
├── ui/                     # UI компоненты
│   └── MainMenu.js        # Главное меню выбора игр
│
└── utils/                  # Утилиты
    └── performance.js     # Оптимизации производительности
```

## 🎯 Архитектура

### Core система

- **GameBase** - базовый класс для всех игр. Определяет стандартный жизненный цикл:
  - `onInit()` - инициализация
  - `onStart()` - начало игры
  - `update(deltaTime)` - обновление логики
  - `render()` - рендеринг
  - `onStop()` - остановка
  - `onCleanup()` - очистка ресурсов

- **GameManager** - управляет играми:
  - Регистрация игр
  - Переключение между играми
  - Управление состоянием

- **EventBus** - централизованная система событий для коммуникации между модулями

### Добавление новой игры

1. Создать класс, наследующийся от `GameBase`:
```javascript
import { GameBase } from '../../core/GameBase.js';

export class MyNewGame extends GameBase {
  constructor() {
    super('my-game', 'My Game', 'Description', '🎮');
  }
  
  onInit() {
    // Инициализация
  }
  
  update(deltaTime) {
    // Игровая логика
  }
  
  render() {
    // Рендеринг
  }
}
```

2. Зарегистрировать в `main.js`:
```javascript
import { MyNewGame } from './games/my-game/MyNewGame.js';
gameManager.registerGame(new MyNewGame());
```

## 🚀 Запуск

Просто откройте `index.html` в браузере. Все модули загружаются автоматически.

## 🎮 Игры

### Frost Click ❄️
- Выживание 10 минут
- Избегайте бомб 💣
- Собирайте подарки 🎁
- Специальные предметы: лед 🧊, Somnia
- Интеграция с Web3 для лидерборда
- **Canvas-версия** для оптимальной производительности (60 FPS с 500+ объектами)

## 🔧 Технологии

- Vanilla JavaScript (ES6+ modules)
- **Canvas API** для высокопроизводительного рендеринга
- Web3.js для блокчейн интеграции
- CSS3 для стилизации UI
- Оптимизированный game loop с deltaTime

## 📝 Примечания

- Все игры изолированы друг от друга
- Каждая игра создает свой UI динамически
- Используется EventBus для слабой связанности модулей
- Canvas API обеспечивает высокую производительность (60 FPS с 500+ объектами)
- Оптимизированный рендеринг с предзагруженными спрайтами

